# Card Vision Skill

Card scan must:
- Start with candidate matches + user confirmation
- Return confidence scores
- Never silently pick a low-confidence match
- Log enough data to debug (without storing raw images long-term)

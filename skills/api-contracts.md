# API Contracts Skill

All APIs must:
- Return JSON
- Use REST conventions
- Use consistent error shape: { error: string }
- Avoid exposing 3rd party response shapes directly
- Validate inputs and fail gracefully
